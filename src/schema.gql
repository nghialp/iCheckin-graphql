# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Checkin {
  id: ID!
  user: User!
  location: String!
  note: String
  checkedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Friendship {
  id: ID!
  requester: User!
  recipient: User!
  status: FriendshipStatus!
  createdAt: DateTime!
}

"""Trạng thái quan hệ bạn bè"""
enum FriendshipStatus {
  PENDING
  ACCEPTED
  REJECTED
}

type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  avatar: String
  points: Float!
  level: Float!
  refreshToken: String!
  sentFriendRequests: [Friendship!]!
  checkins: [Checkin!]!
  receivedFriendRequests: [Friendship!]!
}

type AuthResponse {
  accessToken: String!
  refreshToken: String!
  user: User!
}

type Query {
  getUsers: [User!]!
  getUser(id: String!): User!
  getProfile: User!
  myFriends: [User!]!
  myCheckins: [Checkin!]!
  checkinsAt(location: String!): [Checkin!]!
}

type Mutation {
  signup(input: SignupInput!): AuthResponse!
  login(input: LoginInput!): AuthResponse!
  oauthLogin(input: OAuthInput!): AuthResponse!
  refreshToken(token: String!): AuthResponse!
  sendFriendRequest(toUserId: String!): Friendship!
  acceptFriendRequest(requestId: String!): Friendship!
  unfriend(friendId: String!): Boolean!
  checkin(location: String!, note: String): Checkin!
}

input SignupInput {
  name: String!
  email: String!
  password: String!
}

input LoginInput {
  email: String!
  password: String!
}

input OAuthInput {
  provider: String!
  providerId: String!
  email: String!
  name: String!
  avatar: String
}